# most likely depeciated
class Parser:
	def showParser:
	        f = open('shows.txt', 'r')
	        shows = []
	        for line in f:
	                shows.append(line)
	        return shows
	def rssParse(shows):
		
